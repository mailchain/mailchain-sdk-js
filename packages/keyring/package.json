{
	"name": "@mailchain/keyring",
	"description": "Mailchain keyring",
	"license": "Apache-2.0",
	"author": {
		"name": "Mailchain",
		"email": "support@mailchain.co",
		"url": "https://mailchain.com/"
	},
	"version": "0.8.0",
	"dependencies": {
		"@mailchain/addressing": "0.8.0",
		"@mailchain/crypto": "0.8.0",
		"@mailchain/encoding": "0.8.0"
	},
	"scripts": {
		"build": "cross-env NODE_ENV=production rollup -c --bundleConfigAsCjs",
		"test": "run -T jest",
		"lint:test": "yarn run -T eslint --ext js,ts,tsx src",
		"lint:fix": "yarn run -T eslint --fix --ext js,ts,tsx src"
	}
}
