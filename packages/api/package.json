{
	"name": "@mailchain/api",
	"private": true,
	"sideEffects": false,
	"type": "module",
	"version": "0.1.0",
	"dependencies": {
		"@mailchain/protobuf": "0.1.0",
		"@mailchain/types": "0.1.0",
		"mimetext": "^2.0.7",
		"axios": "^0.26.1"
	},
	"scripts": {
		"test": "jest --passWithNoTests --runInBand",
		"generate": "rm -rf ./src/axios; openapi-generator-cli generate -i ../../docs/openapi/api.json -o ./src/api -g typescript-axios --additional-properties=paramNaming=PascalCase,legacyDiscriminatorBehavior=false",
		"swagger-ui": "swagger-ui ./api.json",
		"lint:test": "yarn eslint --ext js,ts,tsx src",
		"lint:fix": "yarn eslint --fix --ext js,ts,tsx src"
	},
	"devDependencies": {
		"axios-mock-adapter": "^1.20.0",
		"jest-mock-axios": "^4.6.1"
	}
}
